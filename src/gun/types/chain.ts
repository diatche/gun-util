/**
 * A Gun node.
 * 
 * This type is defines as `any` to work in both 0.2019.* and 0.2020.* versions of Gun.
 */
export declare type IGunChainReference<DataType = Record<string, any>, ReferenceKey = any, IsTop extends 'pre_root' | 'root' | false = false> = any;
